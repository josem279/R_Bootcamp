A <- 10
B <- 5

C <- A+B # Spaces in R don't matter

# How do we print out a var in console - in R 
# we just call the var
C

var1 <- 2.5 
var2 <- 4 # In R we can invoke code out of order

# We can perform operations on vars and call functions
result <- var1 / var2

answer <- sqrt(var2)

# Working with chars
greeting <- "Hello"
name <- "Bob"

# In R, we can concat strings with the paste() func.
message <- paste(greeting, name)
message

